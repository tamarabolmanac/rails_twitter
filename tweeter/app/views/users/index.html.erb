<p id="notice"><%= notice %></p>

<h1>Users</h1>

<table>
  <thead>
    <tr>
      <th>Username</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <% if user.id == @current_user.id then %>
      <%else%>
      <tr>
        <td><%= user.username %></td>
        <td><%= link_to 'View profie', user %></td>
        <!--<td><%= link_to 'Edit', edit_user_path(user) %></td>
         <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td> --!>
        
      </tr>
      <% end %>
    <% end %>
  </tbody>
</table>

<br>